apiVersion: v1
kind: Service
metadata:
  name: front-end-service
  namespace: cloudcrew
spec:
  selector:
    app: front-end
  ports:
    - name : front-end-service
      port: 80
      targetPort: 80
